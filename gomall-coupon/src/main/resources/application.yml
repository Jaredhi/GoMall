# 配置数据库
spring:
  datasource:
    username: root
    password: rootroot
    url: jdbc:mysql://192.168.108.131:3306/gomall_sms
    driver-class-name: com.mysql.jdbc.Driver

# 映射mapper文件
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 7000
